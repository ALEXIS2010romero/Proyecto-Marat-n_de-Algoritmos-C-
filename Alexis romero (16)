#include <iostream>
using namespace std;

int main() {
    float base, altura;
    cout << "Ingrese la base y la altura: ";
    cin >> base >> altura;
    cout << "El area es: " << (base * altura) / 2 << endl;
    return 0;
}
